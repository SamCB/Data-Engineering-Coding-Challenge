import { addArticle, getArticle } from '../addArticle';

test('addArticle will add the given article to the database', async ()=> {
  
});

test('addArticle will fail when there is no provided security key', async ()=> {
});

test('addArticle will fail when the security key is incorrect', async ()=> {
});

test('addArticle will fail when there is some essential part missing', async ()=> {
});

test('addArticle will override an old article when given the same article key', async ()=> {
});
